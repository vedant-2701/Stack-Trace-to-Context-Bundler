This is a brand-new project — none of AGENTS.md, constitution.md, or
specs/INDEX.md exist yet. You will create them in this chat, not read them
first.

1. Read my project idea below in full. Interrogate it one question at a
   time, with a suggested answer I can react to — architecture, data, edge
   cases, anything glossed over. Stay at project level: don't design any
   single feature in depth yet. Flag every unstated assumption instead of
   filling it in yourself.

2. Once we reach real shared understanding, propose how the project splits
   into distinct features/modules, including which features depend on
   others being done first. Confirm the split with me before writing
   anything.

3. Write memory/constitution.md — non-negotiable principles derived from
   what we actually discussed. Its stack section states the *rule* only
   ("stack is fixed once chosen, changes require a decision record") — the
   actual stack values go in AGENTS.md, not here.

4. Once the stack is settled, write AGENTS.md (project overview with the
   actual stack values, build/test commands, boundaries, a pointer to
   CONVENTIONS.md — not a copy of it).

5. Write CONVENTIONS.md — idiomatic patterns for the chosen stack: naming,
   error handling, logging, testing.

6. Write specs/INDEX.md listing every feature: one-line description,
   dependencies, status "idea." Do not write any feature's spec.md yet.

7. Write README.md — human-facing project description, pulling stack and
   commands from AGENTS.md rather than restating them differently.

8. Anywhere something is genuinely unresolved, mark it [NEEDS CLARIFICATION]
   and ask — don't guess to keep momentum.

For any high-stakes, hard-to-reverse call, tell me and I'll invoke the
llm-council skill before we lock it in.

My project idea:
<PASTE YOUR IDEA HERE>